# Chat 3 Workspace

## Notice: 2026-06-26 Folder Reorganization

저장소 폴더 정리로 채팅 작업물 저장 위치가 바뀌었습니다.

이 채팅에서 새로 만든 임시 파일, 실험 결과, 중간 산출물은 먼저 아래 형식으로 저장합니다.

```text
work/chat-3/YYYY-MM-DD-topic/
```

검증이 끝난 LSP 도구는 `src/tools/` 또는 `src/lsp/`로 옮기고, 공식 문서나 이력은 `docs/guide/`, `docs/history/`, `docs/investigations/` 중 알맞은 곳으로 옮깁니다.

세 번째 채팅에서 만든 임시 작업물과 실험 결과를 보관합니다.

권장 폴더 이름:

```text
YYYY-MM-DD-topic/
```
